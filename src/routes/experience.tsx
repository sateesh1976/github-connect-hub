import { createFileRoute } from "@tanstack/react-router";
import { Building2, Calendar } from "lucide-react";
import { PageIntro } from "@/components/portfolio-layout";
import { experiences } from "@/lib/portfolio-data";
export const Route = createFileRoute("/experience")({ head: () => ({ meta: [{ title: "Experience | Sateesh Kumar Singh" }, { name: "description", content: "20+ years of technology leadership across AI, banking, automotive, data engineering and cloud platforms." }, { property: "og:title", content: "Experience | Sateesh Kumar Singh" }, { property: "og:description", content: "Technology leadership across AI, data and cloud." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: Experience });
function Experience() { return <section className="section-container py-16"><PageIntro eyebrow="Experience" title="A career built around complex transformation.">Progressive technology leadership across AI, financial services, automotive, telecom and enterprise data platforms.</PageIntro><div className="space-y-5">{experiences.map(([title, company, period, description, tags]) => <article className="glass-card grid gap-5 p-6 md:grid-cols-[220px_1fr]" key={company + period}><div><p className="flex items-center gap-2 font-mono text-sm text-primary"><Calendar className="size-4" />{period}</p><p className="mt-2 flex items-center gap-2 text-sm text-muted-foreground"><Building2 className="size-4" />{company}</p></div><div><h2 className="text-xl font-bold">{title}</h2><p className="mt-3 leading-7 text-muted-foreground">{description}</p><div className="mt-4 flex flex-wrap gap-2">{tags.map(tag => <span className="rounded-sm bg-secondary px-2 py-1 text-xs text-secondary-foreground" key={tag}>{tag}</span>)}</div></div></article>)}</div></section>; }