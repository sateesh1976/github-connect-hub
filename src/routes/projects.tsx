import { createFileRoute } from "@tanstack/react-router";
import { PageIntro } from "@/components/portfolio-layout";
import { projects } from "@/lib/portfolio-data";
export const Route = createFileRoute("/projects")({ head: () => ({ meta: [{ title: "AI & Data Projects | Sateesh Singh" }, { name: "description", content: "Selected enterprise AI, cloud, data platform, automotive and financial services projects led by Sateesh Kumar Singh." }, { property: "og:title", content: "AI & Data Projects | Sateesh Singh" }, { property: "og:description", content: "Selected AI, data and cloud transformation programs." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: Projects });
function Projects() { return <section className="section-container py-16"><PageIntro eyebrow="Selected work" title="Programs delivered across regulated industries.">Enterprise projects spanning data modernization, connected vehicles, investment management and AI-enabled analytics.</PageIntro><div className="grid gap-5 md:grid-cols-2">{projects.map(([title, client, period, description, tech]) => <article className="glass-card p-6" key={title}><p className="font-mono text-xs text-primary">{period}</p><h2 className="mt-3 text-xl font-bold">{title}</h2><p className="mt-1 font-semibold text-primary">{client}</p><p className="mt-4 leading-7 text-muted-foreground">{description}</p><div className="mt-5 flex flex-wrap gap-2">{tech.map(item => <span className="rounded-sm bg-secondary px-2 py-1 text-xs" key={item}>{item}</span>)}</div></article>)}</div></section>; }